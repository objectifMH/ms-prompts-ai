spring:
  ai:
    openai:
      api-key: dummy-test-key-123
      base-url: http://localhost:${wiremock.server.port}
      chat:
        options:
          model: llama-3.3-70b-versatile
logging:
  level:
    org.springframework.ai.chat.client.advisor.SimpleLoggerAdvisor: trace
